# iOS production build — Bloomly

```bash
cd ~/Projects/bloomly
~/Projects/abby/scripts/app_store_preflight.sh && eas build --platform ios --profile production
```

Checklist: `~/Projects/abby/docs/APP_STORE_PREFLIGHT_CHECKLIST.md` — subscription (Shop tab legal text).

**Note:** UI redesign later; build 24 may already be linked in ASC.
